<!DOCTYPE html>
<html lang="en">

<head>
    <%- include ("partials/head.ejs") %>
        <title>Home</title>
</head>

<body>
    <header>
        <h1>Swiper</h1>
    </header>

    <main>
        <ul>
            <% for (let i = 0; i <users.length; i++) { %>
                <li>
                    <figure>
                        <a href="#">
                            <img src="images/users/<%= users[i].photo %>" alt="Profile picture of ">
                            <figcaption>
                                <h2>
                                    <%= users[i].name %>, <%= users[i].age %>
                                </h2>
                                <h3>
                                    <%= users[i].work %>, <%= users[i].id %>
                                </h3>
                                <form action="/match" method="POST">
                                    <label for="dislike">
                                        <input type="submit" name="dislike" class="dislike" onchange="this.form.submit();" data-id="<%= users[i].id %>" value="kots">
                                        <!-- <input type="checkbox" name="dislike" class="dislike" onchange="this.form.submit();" data-id="<%= users[i].id %>"> -->
                                    </label>
                                    <label for="like">
                                        <input type="submit" name="like" class="like" onchange="this.form.submit();" data-id="<%= users[i].id %>" value="hartje">
                                        <!-- <input type="checkbox" name="like" class="like" onchange="this.form.submit();" data-id="<%= users[i].id %>"> -->
                                    </label>
                                    <!-- <div>
                                        <label for="#">
                                            <input type="checkbox">
                                            <span class="check"></span>
                                        </label>
                                    </div>                                     -->
                                    <!-- <input type="submit" value="Verstuur"> -->
                                </form>
                            </figcaption>
                        </a>
                    </figure>
                </li>
                <% } %>
        </ul>
        <p>Er zijn geen gebruikers meer om te laten zien, kom later terug.</p>
    </main>

    <footer>
        <%- include ("partials/footernav-home.ejs") %>
    </footer>
    <script src="scripts/script.js"></script>
</body>

</html>