<!DOCTYPE html>
<html lang="en">

<head>
    <%- include ("partials/head.ejs") %>
        <title>Match-List</title>
</head>

<body class="matchlist">
    <header>
        <h1>Swiper</h1>
    </header>

    <main>
        <ul>
            <% for (let i = 0; i < liked.length; i++) { %>
                <li>
                    <article>
                        <figure>
                            <img src="images/users/<%= liked[i].photo %>" alt="Liked user">
                        </figure>
                        <figcaption>
                            <h2>
                                <%= liked[i].name %>,
                                    <%= liked[i].age %>
                            </h2>
                            <p>You've got a match, lets talk!</p>
                        </figcaption>
                    </article>
                    <div></div>
                </li>
                <% }%>
        </ul>
    </main>
    <footer>
        <%- include ("partials/footernav-matches.ejs") %>
    </footer>
</body>

</html>